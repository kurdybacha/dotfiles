name: cppzmq
root: ~/opensource/cppzmq

windows:
    - editor:
        - systemctl --user start rdm.service
        - vim -c 'set makeprg=cmake\ --build\ build' -c ':FocusDispatch build/bin/unit_tests' zmq.hpp
    - tests:
